1. Open your Python shell (inside your virtual environment):

flask shell

2. Run the following inside the shell:

#copy this

from backend.models import db, User
from werkzeug.security import generate_password_hash

user = User.query.filter_by(username="admin").first()
user.password_hash = generate_password_hash("### put Password here ###")
db.session.commit()
✅ Now you can log in again with:

Username: admin

Password: ### Password here ###

history:
newpassword123