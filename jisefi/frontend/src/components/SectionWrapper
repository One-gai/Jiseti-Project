import React from 'react';
import './SectionWrapper.module.css';

const SectionWrapper = ({ children, className }) => {
    return (
        <div className={`section-wrapper ${className}`}>
            {children}
        </div>
    );
};

export default SectionWrapper;